// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  url: '',
  API_PRODUCTO_URL: 'https://cad-mc-product.herokuapp.com/',
  API_LOGIN_URL: 'http://localhost:3001/',
  API_PERSON_URL: 'https://cad-mc-profile.herokuapp.com/',
  API_OPERATION_URL: 'https://cad-mc-operation.herokuapp.com/',
  API_SURVEY_URL: 'https://cad-mc-survey.herokuapp.com/'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
